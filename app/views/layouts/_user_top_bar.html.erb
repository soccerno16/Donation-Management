<% if !current_user and !current_admin %>
	<%= link_to "Login", new_user_session_path, :title => "Login" %> / 
	<%= link_to "Sign Up", new_user_registration_path, :title => "Sign Up" %>
<% else %>
	<%= link_to "Logout", :controller => '/users', :action => 'sign_out' %>
<% end %>